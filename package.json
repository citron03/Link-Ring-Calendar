{
  "name": "link-ring-calendar",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -w -r run dev",
    "dev:local": "pnpm -w -r run dev:local",
    "build": "pnpm -w -r run build",
    "build:prod": "pnpm -w -r run build:prod",
    "build:dev": "pnpm -w -r run build:dev",
    "start": "pnpm -w -r --parallel run start",
    "start:dev": "pnpm -w -r --parallel run start:dev"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3"
  }
}
